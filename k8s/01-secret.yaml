apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: expense-tracker
type: Opaque
# stringData lets us write plain text; Kubernetes base64-encodes it for us.
# Both services read the same SECRET_KEY so their JWTs stay compatible.
stringData:
  SECRET_KEY: firstappwithfastapi2026
